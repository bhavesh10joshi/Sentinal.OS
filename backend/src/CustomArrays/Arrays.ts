// What we are going to do is that after we successfully parse the tree 
// We are going to break it and store it in chunks inside this array in the form of objects 
// And later in the form of payload , we will pass this to AI . In order to check bugs 
import { CodeBlock } from "../Interfaces/interface";
export const Collections:CodeBlock[] = []; 